---
pubDatetime: 2025-04-28T15:57:52.737Z
modDatetime: 2025-04-28T15:57:52.737Z

title: Graphics, Animations & UI
metadata_title: Graphics, Animations & UI - Python Programming | V.Vidhya
slug: graphics-animations-and-ui
featured: false
# ogImage: https://user-images.githubusercontent.com/53733092/215771435-25408246-2309-4f8b-a781-1f3d93bdf0ec.png
description: Learn Graphics, Animations and UI in Python Programming by practicing exercises based on real-world scenarios with V.Vidhya.

order: 100
---

import Admonition from '@/components/Admonition.astro';
import LinkMd from '@/components/LinkMd.astro';
import CollapsibleSection from '@/components/CollapsibleSection.astro';
import LibFileSrc from "@/assets/courses/programming/python/exercises/graphics-animations-and-ui/make_canvas.py"

<Admonition variant="caution" title="note">
    <div class="flex flex-col gap-2 my-2">
        <p>You can <a href={LibFileSrc} download="make_canvas.py">get the <i>make_canvas()</i></a> utility function, which makes drawing the canvas for painting raw graphics 
        simpler</p>
        {/* <FileDownload fileSrc={LibFileSrc} fileName="simpleimage.py" /> */}
        <p>The SimpleImage module uses the Pillow image library so in order for SimpleImage to work properly, 
        you need to <LinkMd href="https://pillow.readthedocs.io/en/stable/installation/basic-installation.html" title="install Pillow" /> on your machine.</p>
    </div>
        {/* <p class="!my-2.5 lg:!my-5">The required SimpleImage helper module for all the exercises can be downloaded from here:</p>  */}
    {/* <div class="flex flex-col lg:flex-row lg:gap-3 justify-center items-start lg:justify-start lg:items-center">
    </div> */}
</Admonition>

---

#### Q1.

Imagine you're building a simple chess game and your first job right now is to just draw the game board. So create a canvas (for e.g. 800x800)
and draw a 8x8 grid that fits the canvas.

<CollapsibleSection title="Instructions">
- Keep the key parameters - canvas size, number of rows and columns etc. defined in one place so it's easy to change thm later 
(for e.g., to make a larger checkers board).
</CollapsibleSection>

<div class="w-full my-2"></div>

<CollapsibleSection title="Expected Output">
    ![Expected Output Preview](@/assets/courses/programming/python/exercises/graphics-animations-and-ui/1.png)
</CollapsibleSection>

{/* ---

#### Q2.

Imagine you're adding an Instagram-style <LinkMd href='https://en.wikipedia.org/wiki/Sepia_(color)' title='Sepia filter' /> to your photo tool - a warm, brownish tone that gives pictures a nostalgic, old-time feel 
and helps freeze those good memories.

<CollapsibleSection title="Instructions">
- Ask the user for an image filename (or uses a default if they just press Enter) and load the selected image.
- Apply the Sepia filter to every pixel using the following transform (then cap each channel at 255):
    - `new_red = 0.393 * old_red + 0.769 * old_green + 0.189 * old_blue`
    - `new_green = 0.349 * old_red + 0.686 * old_green + 0.168 * old_blue`
    - `new_blue = 0.272 * old_red + 0.534 * old_green + 0.131 * old_blue`
- Show the image before and after applying the filter, so your friend can instantly compare the edited image against the original.
</CollapsibleSection>

<div class="w-full my-2"></div>

<CollapsibleSection title="Expected Output">
- Original image:
    ![Original Image Preview](@/assets/courses/programming/python/exercises/images/7.jpg)

- Edited image:  
    ![Edited Image Preview](@/assets/courses/programming/python/exercises/images/8.jpg)
</CollapsibleSection>

---

#### Q3.

You're working with environmental scientists who need an easy way to spot active forest fires in a satellite 
photo of <LinkMd href="https://en.wikipedia.org/wiki/Wildfires_in_2017" title="Greenland's 2017 fire season" />. Your job is to write a function that lights up 
the burning areas in bright red and turns the rest of the landscape into shades of gray, so the fires jump right off the screen.

<CollapsibleSection title="Instructions">
- Ask the user for an image filename (or uses a default if they just press Enter) and loads the selected image.
- For each pixel, compute the average of its red, green and blue values. If the pixel's red component is greater than equal to that average, it's sufficiently red, indicating fire.
- **Highlighting Fires:** 
    - If a pixel is sufficiently red: set red = 255, green = 0, blue = 0
    - else compute the pixel's grayscale value by averaging its RGB channels and set red = green = blue = that average.
- Finally, show the original image side by side with the processed version for comparison.
</CollapsibleSection>

<div class="w-full my-2"></div>

<CollapsibleSection title="Expected Output">
- Original image:
    ![Original Image Preview](@/assets/courses/programming/python/exercises/images/3.jpg)

- Edited image:  
    ![Edited Image Preview](@/assets/courses/programming/python/exercises/images/4.jpg)
</CollapsibleSection>

---

#### Q4.

You're building a simple photo-editing feature for a nature website. 
Your friend wants to add a mirror reflection effect beneath each photo, as if the image is sitting on a calm lake. 
The reflected image should flip the original vertically, creating a seamless reflection.

<CollapsibleSection title="Instructions">
- Ask the user for an image filename (or uses a default if they just press Enter) and loads the selected image.
- Create a new blank image that is twice as tall as the original (same width), copies the original into the top half, then
generate a vertical reflection of the original into the bottom half, so the first row of the original becomes the last row of the output and so on.
- Show the image before and after applying the filter, so your friend can instantly compare the edited image against the original.
</CollapsibleSection>

<div class="w-full my-2"></div>

<CollapsibleSection title="Expected Output">
- Original image:
    ![Original Image Preview](@/assets/courses/programming/python/exercises/images/5.jpg)

- Edited image:
    ![Edited Image Preview](@/assets/courses/programming/python/exercises/images/6.jpg)
</CollapsibleSection> */}